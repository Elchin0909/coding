# PostgreSQL ? Railway o'zgaruvchilaridan
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/coding}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:parol}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Redis ? Railway o'zgaruvchilaridan
spring.data.redis.host=${REDISHOST:localhost}
spring.data.redis.port=${REDISPORT:6379}

# JWT
jwt.secret=${JWT_SECRET:bu-juda-uzun-maxfiy-kalit-kamida-32-belgi-bolsin-256bit}
jwt.expiration=900000
jwt.refresh-expiration=604800000

# Railway port
server.port=${PORT:8080}